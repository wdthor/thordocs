# Performance

## Tree shaking

- Allows unused code or dead code to be removed from the JavaScript bundle
- Prefer tree-shaking libs
  - `lodash-es` (for more modern browsers - ES6) instead of `lodash`
- Use [BundleJS](https://bundlejs.com/) to check npm package sizes
